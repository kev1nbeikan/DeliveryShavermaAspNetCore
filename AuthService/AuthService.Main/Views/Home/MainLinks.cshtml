@model Microsoft.Extensions.Options.IOptions<BarsGroupProjectN1.Core.AppSettings.ServicesOptions>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <title>.Net SHAVA🥙</title>
    <style>
        .menu-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); /* Responsive grid */
            grid-gap: 20px;
            margin-top: 30px;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f5f5f5;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
        }

        .container {
            background-color: #fff;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 500px;
            width: 100%;
        }
        .btn {
            background-color: #FFC300; /* Более приятный желтый */
            color: #fff;
            padding: 15px 30px;
            border: none;
            border-radius: 5px;
            font-size: 1.1rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #E6A700; /* Более темный оттенок желтого при наведении */
        }

        .logo {
            width: 150px;
            height: 150px;
            margin-bottom: 30px;
            border-radius: 50%;
            overflow: hidden;
            background-color: #eee;
        }

        .logo img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .error-code {
            background-color: #f0f0f0;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 1.2rem; /* Более крупный шрифт */
            font-weight: bold; /* Жирный шрифт */
            color: #666;
            text-transform: uppercase; /* Заглавные буквы */
            margin-bottom: 20px; /* Отступ снизу */
        }

        .shawarma-smiley {
            font-size: 100px; /* Adjust size as needed */
            color: #ffb61e;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
<div class="container">
    <h1 class="text-center mb-4">.Net SHAVA🥙</h1>

    <div class="error-code">
        <span class="shawarma-smiley">🌯</span>
        <h2>Сервисы</h2>
    </div>

    <div class="menu-grid">
        <a href="@Model.Value.OrderUrl/swagger" class="btn">
            <span class="shawarma-smiley">🛍️</span>
            Заказы
        </a>
        <a href="@Model.Value.CouriersUrl" class="btn">
            <span class="shawarma-smiley">🚴</span>
            Курьеры
        </a>
        <a href="@Model.Value.StoreUrl" class="btn">
            <span class="shawarma-smiley">🏪</span>
            Магазины
        </a>
        <a href="@Model.Value.UsersUrl" class="btn">
            <span class="shawarma-smiley">👤</span>
            Пользователи
        </a>
        <a href="@Model.Value.MenuUrl" class="btn">
            <span class="shawarma-smiley">🍔</span>
            Меню
        </a>
        <a href="@Model.Value.AuthUrl" class="btn">
            <span class="shawarma-smiley">🔑</span>
            Авторизация
        </a>
        <a href="@Model.Value.PaymentOrchestratorUrl" class="btn">
            <span class="shawarma-smiley">💳</span>
            Платежи
        </a>
        <a href="@Model.Value.ChatUrl" class="btn">
            <span class="shawarma-smiley">💬</span>
            Чат
        </a>
    </div>
</div>
</body>
</html>